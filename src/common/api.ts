import { SERVER_URL } from './constant';

export const API_USER_UNIQUEID = `${SERVER_URL}/user/token`;
export const API_VERSION_LATEST = `${SERVER_URL}/env/latest`;
export const API_VERSION_AVAILABLE = `${SERVER_URL}/env/available`;
export const API_UPDATE_URL = `${SERVER_URL}/update/url`;